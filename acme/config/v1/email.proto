syntax="proto3";

package acme.config.v1;

message Email {
  // 邮箱账号
  string email = 1;
  // 邮箱密码
  string password = 2;
  // 邮箱SMTP地址
  string smtp_address = 3;
  // 邮箱SMTP端口
  int32 smtp_port = 4;
}