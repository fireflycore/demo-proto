syntax = "proto3";

package acme.config.v1;

// Email 定义邮件通道配置。
message Email {
  // 邮箱账号。
  string email = 1;
  // 邮箱密码。
  string password = 2;
  // SMTP 服务地址。
  string smtp_address = 3;
  // SMTP 服务端口。
  int32 smtp_port = 4;
}
