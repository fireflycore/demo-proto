syntax = "proto3";

package acme.config.v1;

import "acme/basic/v1/tls.proto";

// Etcd 定义 Etcd 作为底层存储时的连接参数。
message Etcd {
  // Etcd 访问用户名。
  string username = 1;
  // Etcd 访问密码。
  string password = 2;
  // Etcd TLS 配置。
  basic.v1.TLS tls = 3;
  // Etcd 节点地址列表。
  repeated string endpoint = 4;
}
