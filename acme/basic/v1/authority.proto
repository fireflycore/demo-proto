syntax="proto3";

package acme.basic.v1;

// 主体类型
enum SubjectType {
  SUBJECT_TYPE_UNSPECIFIED = 0;
  // 用户
  SUBJECT_TYPE_USER = 1;
  // 角色
  SUBJECT_TYPE_ROLE = 2;
  // 部门
  SUBJECT_TYPE_DEPT = 3;
  // 部门
  SUBJECT_TYPE_ORG = 4;
  // 接口
  SUBJECT_TYPE_API = 5;
}

// 接口白名单：网关层需要一个动态的接口白名单（无需token验证的）：
// - 请求定义：主体，客体，动作
// - 主体为*的时候则是全部都可以访问，主体为特定IP的时候则是特定IP可以访问