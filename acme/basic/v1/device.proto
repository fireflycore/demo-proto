syntax="proto3";

package acme.basic.v1;

// 设备形态
enum DeviceFormFactor {
  DEVICE_FORM_FACTOR_UNSPECIFIED = 0;
  // 桌面设备
  DEVICE_FORM_FACTOR_DESKTOP = 1;
  // 平板设备
  DEVICE_FORM_FACTOR_TABLET = 2;
  // 手机设备
  DEVICE_FORM_FACTOR_PHONE = 3;
  // 扩展现实设备
  DEVICE_FORM_FACTOR_XR = 4;
  // 物联网设备
  DEVICE_FORM_FACTOR_IOT = 5;
}